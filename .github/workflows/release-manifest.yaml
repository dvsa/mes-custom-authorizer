name: Create Release Manifest
on:
  workflow_dispatch:
  push:
    tags:
      - release-*

permissions:
  id-token: write
  contents: write

jobs:
  release-manifest:
    uses: dvsa/des-workflow-actions/.github/workflows/release-manifest.yaml@MES-10606_Fix_Release_Manifest
    with:
      component: 'api'
    secrets: inherit
